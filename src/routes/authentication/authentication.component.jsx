import SignInForm from '../../components/sign-in-form/sign-in-form.component';

const Authentication = () => {
    return (
        <div 
        className='items-center justify-center m-auto relative min-h-screen'>
           <SignInForm/> 
        </div>
    );
};

export default Authentication;